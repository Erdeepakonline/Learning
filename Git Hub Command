Github Useful Command :
--------------------------
…or create a new repository on the command line:

echo "# Git-Hub-Learning" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Erdeepakonline/Git-Hub-Learning.git
git push -u origin main


…or push an existing repository from the command line :

git remote add origin https://github.com/Erdeepakonline/Git-Hub-Learning.git
git branch -M main
git push -u origin main


User kaun use kar rah hai command :

git config --global user.email "email"
git config --global user.name "username"

then initialized process write command here : get init   #press enter button

git status

git add -A

get commit -m "index.html added"

git remote add origin https://github.com/Erdeepakonline/My-Learning.git


touch contact.html

git checkout -f

git checkout

git log : aapne commit kya kiya aur kisane kiya

git log -1

ls command

git rm waste.html

git rm--cached waste.html
